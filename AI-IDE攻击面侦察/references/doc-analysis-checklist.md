# 文档分析清单侦察阶段看文档时，用这个清单避免漏掉关键面。

# MCP与工具集成

|检查项|审查重点|如果文档没写清|后续skill|
|---|---|---|---|
|MCP是否支持工作区配置|配置路径、示例、是否自动发现|高度可疑，优先黑盒测| `MCP配置投毒测试` |
|工具调用是否有审批|审批粒度、记忆方式、UI提示|可能存在弱审批| `MCP配置投毒测试`、`终端过滤绕过测试` |

# 规则、提示、指令文件

|检查项|审查重点|如果文档没写清|后续skill|
|---|---|---|---|
|自动读取哪些规则文件|路径、格式、优先级|可能存在隐藏自动加载点| `提示词注入攻击链测试` |
|规则是否覆盖用户意图|合并顺序、作用域|可能存在高危PI面| `提示词注入攻击链测试` |

# 终端与命令执行

|检查项|审查重点|如果文档没写清|后续skill|
|---|---|---|---|
|是否支持直接跑命令|Shell、task、终端、Agent mode|需要实际测执行链| `终端过滤绕过测试` |
|是否宣称有高强度安全模式|限制粒度、平台差异|往往实现很弱| `终端过滤绕过测试` |

# 网络与外部资源

|检查项|审查重点|如果文档没写清|后续skill|
|---|---|---|---|
|是否会抓取URL、图片、schema|自动还是手动触发|可能存在外传面| `AI IDE数据外传测试` |
|是否支持自定义基址URL、路径或服务名|配置来源、继承范围|可能有配置劫持| `AI IDE数据外传测试` |

# Hook、设置、自动执行

|检查项|审查重点|如果文档没写清|后续skill|
|---|---|---|---|
|是否支持Hook|事件、配置位置、默认行为|高优先级执行面| `AI IDE代码执行面测试` |
|工作区设置能否影响工具路径|示例、限制、审批|可能升级到RCE| `AI IDE代码执行面测试` |

